<?xml version="1.0" encoding="utf-8"?>

<!-- TODO number values ... -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_recycler_item"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_height"
    android:background="@drawable/ripple_effect_primary"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:onClick="OnClickServerItem"
    android:orientation="horizontal"
    android:paddingRight="13dp"
    android:paddingLeft="15dp"
    android:showDividers="middle"
    tools:showIn="@layout/activity_main_content">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="2"
        android:orientation="vertical">

        <TextView
            android:layout_marginTop="2dp"
            android:id="@+id/main_recycler_item_main_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Main text"
            android:textSize="25sp" />

        <TextView
            android:id="@+id/main_recycler_item_secondary_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="(No user name)"
            android:textSize="15sp" />

    </LinearLayout>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_storage_gray" />

    <!--</LinearLayout>-->

</LinearLayout>